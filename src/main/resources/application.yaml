server:
  port: 8081

spring:
  application:
    name: product
  datasource:
    url: jdbc:mysql://localhost:3306/QuanLyCuaHang
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
#    show-sql: true
    properties:
      hibernate:
        format_sql: true

